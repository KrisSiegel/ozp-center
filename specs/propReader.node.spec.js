/**
    Unit tests for the property reader.
*/
module.exports = (function (Ozone) {
//    describe("Property Reader", function () {
//        it("propReader.js", function () {
//            var fs = require("fs");
//            var path = require("path");
//            var os = require("os");
//            fs.writeFileSync(path.join(os.tmpdir(), "test.properties"), "my.property=15", { encoding: "utf8" });
//
//            var props = require("../propReader")(os.tmpdir());
//            expect(props).not.toBe(undefined);
//            expect(props.prop("test")["my.property"]).toBe("15");
//            expect(props.prop("test")["my.otherProperty"]).toBe(undefined);
//        });
//    });
});
